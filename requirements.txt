# LLM Providers
openai>=2.0.0        # Usado para Groq e Ollama (API-compatible)
anthropic>=0.83.0    # Opcional: Claude (requer API key)
python-dotenv>=1.0.0

# Audio capture & VAD
sounddevice>=0.4.6
webrtcvad-wheels>=2.0.14  # Windows-compatible (use 'webrtcvad' on Linux/Mac)
numpy>=1.26.0

# STT (Speech-to-Text) - local
faster-whisper>=1.2.0

# TTS (Text-to-Speech)
edge-tts>=7.0.0     # High quality, requires internet
pyttsx3>=2.90       # Offline fallback
pygame>=2.6.0       # Audio playback for edge-tts

# Vision
mss>=9.0.0          # Screen capture
Pillow>=10.0.0      # Image processing
pytesseract>=0.3.10 # OCR (requires Tesseract installed separately)

# System context
PyGetWindow>=0.0.9  # Active window info (Windows/Linux/Mac)
pyperclip>=1.8.0    # Clipboard access

# UI / Terminal
rich>=13.0.0        # Beautiful terminal output

# Audio processing
scipy>=1.11.0       # WAV file handling

# Google Docs (modo anotação)
google-api-python-client>=2.100.0
google-auth-httplib2>=0.1.1
google-auth-oauthlib>=1.1.0
